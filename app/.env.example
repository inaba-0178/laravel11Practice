APP_NAME=Laravel
APP_ENV=local
APP_KEY=base64:qo7Gzx91QAcpi6z2R8zr+GgeiouW/ESW2oWDMU/AxK4=
APP_DEBUG=true
APP_URL=http://laravel11Practice.local:81

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=mst-db
DB_PORT=3306
DB_DATABASE=mst_db
DB_USERNAME=admin
DB_PASSWORD=mst_pass

SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
# QUEUE_CONNECTION=database
QUEUE_CONNECTION=sync

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# MAIL_MAILER=log
# MAIL_SCHEME=null
# MAIL_HOST=127.0.0.1
# MAIL_PORT=2525
# MAIL_USERNAME=null
# MAIL_PASSWORD=null
# MAIL_FROM_ADDRESS="hello@example.com"
# MAIL_FROM_NAME="${APP_NAME}"

# メール設定
MAIL_MAILER=smtp
MAIL_SCHEME=null
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME= # ここはメールを送るGmailアドレス
MAIL_PASSWORD= # ここはアプリパスワードで作成したパスワード
MAIL_FROM_ADDRESS= # ここもメールを送るGmailアドレス
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

# MST DB
MST_DATABASE=mst_db
MST_ROOT_PASSWORD=root_mst
MST_USER=admin
MST_PASSWORD=mst_pass
DB_MST_HOST=mst
DB_MST_PORT=3307


# USER DB  
USER_DATABASE=user_db
USER_ROOT_PASSWORD=root_user
USER_USER=user_user
USER_PASSWORD=user_pass
DB_USER_HOST=user
DB_USER_PORT=3306

# MST Backup DB
MST_BACKUP_DATABASE=mst_backup_db
MST_BACKUP_ROOT_PASSWORD=root_mst_backup
MST_BACKUP_USER=admin
MST_BACKUP_PASSWORD=mst_backup_pass
DB_MST_BACKUP_HOST=mst-backup-db
DB_MST_BACKUP_PORT=3306

# Dockerユーザー設定
UID=1000
GID=1000

TZ=Asia/Tokyo

REVERB_APP_ID=my-app-id
REVERB_APP_KEY=my-app-key
REVERB_APP_SECRET=my-app-secret
REVERB_HOST=localhost
REVERB_PORT=8085
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"

# vue側のURL
FRONTEND_URL=http://localhost:8080
# 管理ツールURL
ADMIN_URL=http://localhost:81/admin

# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_DEFAULT_REGION=us-east-1
# AWS_BUCKET=
# AWS_USE_PATH_STYLE_ENDPOINT=false

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Laravel ファイルストレージ
FILESYSTEM_DISK=s3
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=car-images
AWS_ENDPOINT=http://minio:9000
AWS_USE_PATH_STYLE_ENDPOINT=true
AWS_URL=http://localhost:9000/car-images